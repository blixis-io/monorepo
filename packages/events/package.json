{
  "name": "@blixis/events",
  "version": "0.0.0",
  "description": "Blixis events: event definition registry, envelopes, and the event bus (transports: in-process, queue, outbox)",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./outbox": {
      "types": "./dist/outbox/index.d.ts",
      "default": "./dist/outbox/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --root ../.. packages/events"
  },
  "dependencies": {
    "@blixis/contracts": "workspace:*",
    "@blixis/database": "workspace:*",
    "@blixis/kernel": "workspace:*",
    "@blixis/shared": "workspace:*",
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "@blixis/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "@types/pg": "catalog:",
    "hono": "catalog:",
    "pg": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "zod": "catalog:"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/blixis-io/monorepo.git",
    "directory": "packages/events"
  }
}
