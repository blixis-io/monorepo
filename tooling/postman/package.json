{
  "name": "@blixis/postman",
  "version": "0.0.0",
  "private": true,
  "description": "Postman collection and environments for the Blixis API, checked against the registered routes",
  "type": "module",
  "scripts": {
    "test": "vitest run --root ../.. tooling/postman"
  },
  "devDependencies": {
    "@blixis/auth": "workspace:*",
    "@blixis/database": "workspace:*",
    "@blixis/events": "workspace:*",
    "@blixis/kernel": "workspace:*",
    "@blixis/tsconfig": "workspace:*",
    "@blixis/users": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
